# Copy this file to `.env` and fill in your Firebase web config.
# On Android/iOS, google-services.json and GoogleService-Info.plist are used,
# but we still read these for non-mobile platforms or when initializing with options.

API_KEY=
APP_ID=
MESSAGING_SENDER_ID=
PROJECT_ID=
AUTH_DOMAIN=
STORAGE_BUCKET=
MEASUREMENT_ID=
